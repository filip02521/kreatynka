---
import BaseLayout from '../layouts/BaseLayout.astro';
import products from '../data/products.json';
---
<BaseLayout title="Kreatynka – Zestawy do tworzenia">
  <section class="py-10 bg-[#232946] text-white">
    <div class="max-w-6xl mx-auto grid md:grid-cols-2 gap-8 items-center">
      <div>
        <h1 class="text-5xl font-bold mb-6 drop-shadow-xl">Wyjątkowe zestawy DIY – twórz, baw się, odkrywaj!</h1>
        <p class="text-xl mb-8">Odkryj prawdziwą przyjemność z tworzenia — gotowe boxy na prezent lub kreatywne popołudnie!</p>
        <a href="#produkty" class="btn text-lg">Zobacz nasze zestawy</a>
      </div>
      <div class="relative w-full overflow-hidden rounded-2xl shadow-2xl">
        <div class="flex gap-2 animate-scroll-gallery">
          <img src="https://images.pexels.com/photos/1335971/pexels-photo-1335971.jpeg?auto=compress&w=500&h=350&fit=crop" class="rounded-xl shadow-lg w-64 h-44 object-cover" alt="Bransoletka DIY" />
          <img src="https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=500&q=80" class="rounded-xl shadow-lg w-64 h-44 object-cover" alt="Świeca DIY" />
          <img src="https://images.unsplash.com/photo-1512436991641-6745cdb1723f?auto=format&fit=crop&w=500&q=80" class="rounded-xl shadow-lg w-64 h-44 object-cover" alt="Makrama DIY" />
        </div>
      </div>
    </div>
    <style>
      @keyframes scroll-gallery {
        0% { transform: translateX(0); }
        100% { transform: translateX(-33.33%); }
      }
      .animate-scroll-gallery {
        animation: scroll-gallery 20s linear infinite alternate;
      }
    </style>
  </section>

  <section id="produkty" class="p-12 bg-[#f4f6fb]">
    <h2 class="text-3xl font-semibold text-center mb-10 text-[#232946]">Nasze zestawy DIY</h2>
    <div class="grid gap-10 sm:grid-cols-2 lg:grid-cols-3">
      {products.map(p => (
        <div class="border border-[#eebbc3] rounded-2xl overflow-hidden shadow-lg hover:shadow-2xl transition bg-white">
          <img src={p.image} alt={p.title} class="w-full h-56 object-cover" />
          <div class="p-6">
            <h3 class="text-xl font-semibold mb-2 text-[#232946]">{p.title}</h3>
            <p class="text-sm text-gray-600 mb-4">{p.description}</p>
            <p class="text-xl font-bold mb-4 text-[#232946]">{p.price} zł</p>
            <button class="snipcart-add-item btn w-full"
              data-item-id={p.slug}
              data-item-price={p.price}
              data-item-url="/"
              data-item-description={p.description}
              data-item-image={p.image}
              data-item-name={p.title}>
              Dodaj do koszyka
            </button>
          </div>
        </div>
      ))}
    </div>
  </section>

  <section class="bg-[#232946] text-white py-16">
    <div class="max-w-5xl mx-auto text-center">
      <h2 class="text-3xl font-semibold mb-8">Dlaczego Kreatynka?</h2>
      <div class="grid gap-10 sm:grid-cols-2 md:grid-cols-4">
        <div>
          <p class="text-xl font-bold mb-2">Eleganckie opakowanie</p>
          <p class="text-sm">Zestawy zapakowane z dbałością o detale, gotowe na prezent.</p>
        </div>
        <div>
          <p class="text-xl font-bold mb-2">Kompletne materiały</p>
          <p class="text-sm">Wszystko, czego potrzebujesz, w jednym eleganckim boxie.</p>
        </div>
        <div>
          <p class="text-xl font-bold mb-2">Ręczne kompletowanie</p>
          <p class="text-sm">Z sercem i precyzją – każdy zestaw przygotowujemy ręcznie.</p>
        </div>
        <div>
          <p class="text-xl font-bold mb-2">Idealny na prezent</p>
          <p class="text-sm">Zachwyt gwarantowany – zarówno dla siebie, jak i bliskich.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="p-12 bg-[#f4f6fb]">
    <h2 class="text-2xl font-semibold text-center mb-4 text-[#232946]">Dołącz do naszego newslettera</h2>
    <p class="text-center text-gray-600 mb-6">Zapisz się, aby otrzymywać inspiracje, rabaty i nowości!</p>
    <form class="flex flex-col sm:flex-row justify-center items-center gap-4 max-w-xl mx-auto">
      <input type="email" placeholder="Twój email" class="p-3 border rounded-xl w-full sm:w-auto flex-1" required />
      <button type="submit" class="btn">Zapisz się</button>
    </form>
  </section>
</BaseLayout>
